import type { WaAuthCredentials } from 'zapo-js/auth';
import type { WaAuthStore } from 'zapo-js/store';
import { BaseSqliteStore } from './BaseSqliteStore';
import type { WaSqliteStorageOptions } from './types';
export declare class WaAuthSqliteStore extends BaseSqliteStore implements WaAuthStore {
    constructor(options: WaSqliteStorageOptions);
    load(): Promise<WaAuthCredentials | null>;
    save(credentials: WaAuthCredentials): Promise<void>;
    clear(): Promise<void>;
}
