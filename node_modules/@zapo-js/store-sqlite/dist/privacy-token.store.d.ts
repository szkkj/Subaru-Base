import type { WaPrivacyTokenStore, WaStoredPrivacyTokenRecord } from 'zapo-js/store';
import { BaseSqliteStore } from './BaseSqliteStore';
import type { WaSqliteStorageOptions } from './types';
export declare class WaPrivacyTokenSqliteStore extends BaseSqliteStore implements WaPrivacyTokenStore {
    constructor(options: WaSqliteStorageOptions);
    upsert(record: WaStoredPrivacyTokenRecord): Promise<void>;
    upsertBatch(records: readonly WaStoredPrivacyTokenRecord[]): Promise<void>;
    getByJid(jid: string): Promise<WaStoredPrivacyTokenRecord | null>;
    deleteByJid(jid: string): Promise<number>;
    clear(): Promise<void>;
    private upsertRow;
    private rowToRecord;
}
