import type { WaMessageSecretEntry, WaMessageSecretStore } from 'zapo-js/store';
import { BaseSqliteStore } from './BaseSqliteStore';
import type { WaSqliteStorageOptions } from './types';
export declare class WaMessageSecretSqliteStore extends BaseSqliteStore implements WaMessageSecretStore {
    private readonly ttlMs;
    private readonly batchSize;
    constructor(options: WaSqliteStorageOptions, ttlMs?: number, batchSize?: 500);
    get(messageId: string, nowMs?: number): Promise<WaMessageSecretEntry | null>;
    getBatch(messageIds: readonly string[], nowMs?: number): Promise<readonly (WaMessageSecretEntry | null)[]>;
    set(messageId: string, entry: WaMessageSecretEntry): Promise<void>;
    setBatch(entries: readonly {
        readonly messageId: string;
        readonly entry: WaMessageSecretEntry;
    }[]): Promise<void>;
    cleanupExpired(nowMs: number): Promise<number>;
    clear(): Promise<void>;
    private upsertRow;
    private deleteMessageSecretsByIds;
}
