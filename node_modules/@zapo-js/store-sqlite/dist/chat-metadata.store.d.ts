import type { WaChatMetadataSnapshot, WaChatMetadataStore } from 'zapo-js/store';
import { BaseSqliteStore } from './BaseSqliteStore';
import type { WaSqliteStorageOptions } from './types';
export declare class WaChatMetadataSqliteStore extends BaseSqliteStore implements WaChatMetadataStore {
    private readonly ttlMs;
    constructor(options: WaSqliteStorageOptions, ttlMs?: number);
    upsertChatMetadata(snapshot: WaChatMetadataSnapshot): Promise<void>;
    getChatMetadata(chatJid: string, nowMs?: number): Promise<WaChatMetadataSnapshot | null>;
    deleteChatMetadata(chatJid: string): Promise<number>;
    cleanupExpired(nowMs: number): Promise<number>;
    clear(): Promise<void>;
}
