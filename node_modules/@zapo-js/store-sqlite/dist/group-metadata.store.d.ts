import type { WaGroupMetadataSnapshot, WaGroupMetadataStore } from 'zapo-js/store';
import { BaseSqliteStore } from './BaseSqliteStore';
import type { WaSqliteStorageOptions } from './types';
export declare class WaGroupMetadataSqliteStore extends BaseSqliteStore implements WaGroupMetadataStore {
    private readonly ttlMs;
    constructor(options: WaSqliteStorageOptions, ttlMs?: number);
    upsertGroupMetadata(snapshot: WaGroupMetadataSnapshot): Promise<void>;
    getGroupMetadata(groupJid: string, nowMs?: number): Promise<WaGroupMetadataSnapshot | null>;
    deleteGroupMetadata(groupJid: string): Promise<number>;
    cleanupExpired(nowMs: number): Promise<number>;
    clear(): Promise<void>;
}
