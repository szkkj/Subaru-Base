import { type SignalAddress, type SignalSessionRecord } from 'zapo-js/signal';
import type { WaSessionStore as WaSessionStoreContract } from 'zapo-js/store';
import { BaseSqliteStore } from './BaseSqliteStore';
import type { WaSqliteStorageOptions } from './types';
interface WaSessionSqliteStoreOptions {
    readonly hasSessionBatchSize?: number;
}
export declare class WaSessionSqliteStore extends BaseSqliteStore implements WaSessionStoreContract {
    private readonly hasSessionBatchSize;
    constructor(options: WaSqliteStorageOptions, storeOptions?: WaSessionSqliteStoreOptions);
    hasSession(address: SignalAddress): Promise<boolean>;
    hasSessions(addresses: readonly SignalAddress[]): Promise<readonly boolean[]>;
    getSession(address: SignalAddress): Promise<SignalSessionRecord | null>;
    getSessionsBatch(addresses: readonly SignalAddress[]): Promise<readonly (SignalSessionRecord | null)[]>;
    setSession(address: SignalAddress, session: SignalSessionRecord): Promise<void>;
    setSessionsBatch(entries: readonly {
        readonly address: SignalAddress;
        readonly session: SignalSessionRecord;
    }[]): Promise<void>;
    deleteSession(address: SignalAddress): Promise<void>;
    clear(): Promise<void>;
    private upsertSession;
}
export {};
